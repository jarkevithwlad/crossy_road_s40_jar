# Generated build output
/build/

# Optional local build products (JAR/JAD files are reproducible from the sources)
/dist/

# Emulator binaries, logs and screenshots (source publication does not need them)
